langgraph>=0.2.0
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-core>=0.3.0
pydantic>=2.0
pyyaml>=6.0
rich>=13.0
python-dotenv>=1.0.0

# Hybrid RAG
chromadb>=0.5.0
sentence-transformers>=3.0.0
numpy>=1.26.0
rank-bm25>=0.2.2

# Utils
tqdm>=4.66.0

# System dependency (not pip): ripgrep (rg)

# Optional / future — AST-based chunking (not used by the regex chunker yet):
# tree-sitter>=0.22.0
# tree-sitter-java>=0.21.0
# tree-sitter-cpp>=0.22.0
